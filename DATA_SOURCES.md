# 데이터 출처

## 셔틀 노선과 시간표

사용자가 제공한 기존 HTML 데이터에서 변환했습니다. 공개 저장소에 배포하기 전, 저장소 소유자가 해당 셔틀 데이터의 공개·재배포 권한과 사내 정보 공개 기준을 확인해야 합니다.

## 아파트 기본 정보

사용자가 제공한 `v2_5` 데이터에서 단지명, 좌표, 세대수, 준공일 같은 기본 정보를 가져왔습니다. 저장소는 네이버 호가·매물 스냅샷을 포함하지 않으며 네이버 비공식 API를 호출하지 않습니다. 단지별 네이버 부동산 URL은 사용자가 현재 매물을 직접 확인하기 위한 외부 링크입니다.

## 아파트 실거래가

`public/data/prices.json`은 사용자가 제공한 `v2_5`의 국토부 공개자료 스냅샷을 초기 참고값으로 사용합니다. 이 스냅샷은 거래별 실제 전용면적을 포함하지 않아 평당가는 면적 구간값으로 근사하며, API 갱신분은 거래별 실제 전용면적으로 계산합니다. 스냅샷은 단지 ID 기준 파생값이며 최신 API의 단지명·지역 동시 검증 결과와 구분해 표시합니다. 원본 파일명·생성일·SHA-256은 `config/price-snapshot.json`에 고정하며 `npm run prices:import-snapshot -- <원본 경로>`로 동일한 파생 데이터를 다시 만들 수 있습니다. 자동 갱신 원본은 공공데이터포털의 국토교통부 아파트 매매 실거래가 API입니다. GitHub Actions는 최근 12개월 거래를 받아 좌표 기반 시군구 코드와 단지명이 일치하거나, 시군구·준공연도·면적 구간·단지명 꼬리로 유일하게 검증되거나, 명시적 별칭이 등록된 경우만 가격을 갱신합니다.

## 행정구역 경계

`config/sgg.json`의 출처, 리비전, 라이선스는 `config/LICENSE-sgg-data`에 기록했습니다.
