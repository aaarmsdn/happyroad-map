# 하이로드 디자인 기준

## 제품 성격

셔틀 정류장과 아파트를 지도에서 빠르게 비교하는 업무형 도구다. 지도와 데이터가 주인공이며 장식은 최소화한다.

## 시각 체계

- 정류장: 남색 사각형, 버스 아이콘. 출근은 오른쪽, 퇴근은 왼쪽, 양방향은 좌우 화살표를 버스 위에 겹쳐 표시하고 정류장은 항상 선명하게 유지한다. 노선 종류 색상은 작은 하단 표시로만 사용한다.
- 아파트: 산호색 원형 건물 아이콘. `전체` 셔틀 필터에서 주변 출근·퇴근 중 한 방향만 만족하면 45% 불투명도로 낮추고, 양방향을 모두 만족하면 선명하게 표시한다. 색상 기준은 `평당가 / 왕복 통근시간 / 단색` 3분할 설정으로 전환한다. 평당가는 2,500만·4,000만·6,000만·8,000만원, 왕복 통근시간은 120·150·180·240분 경계의 초록·파랑·노랑·주황·빨강 5단계로 표시한다. 통근시간은 통상출근과 회사 18:00 퇴근을 우선하고, 없으면 각각 회사 도착 08:00과 출발 18:00에 가장 가까운 운행을 사용한다. 각 방향에서 도보와 셔틀 합계가 가장 짧은 정류장을 독립 선택해 합산하며 한 방향 운행이 없으면 회색으로 표시한다. 확대 시 실거래가 총액 배지는 색상 기준과 무관하게 유지한다.
- 대표 실거래가는 최고값을 기본으로 하고, 아파트 필터의 3분할 세그먼트에서 최고값·평균값·최저값을 전환한다. 지도 총액, 평당가 색상, 상세 가격은 항상 같은 기준을 사용한다.
- 숫자 클러스터도 정류장은 사각형, 아파트는 원형을 유지한다. 아파트 클러스터는 선택된 색상 기준의 구성 단지 평균값을 쓰고 실제 단지 좌표 위에 표시한다.
- 학교: 기본 비표시인 독립 지도 레이어다. 초등학교는 초록 원형, 중학교는 파랑 사각형, 고등학교는 자주색 마름모 안에 학교 아이콘과 급별 글자를 표시한다. 저배율에서는 급별 구성을 유지한 클러스터로 합치고 현재 지도 영역의 학교만 렌더링한다.
- 표면은 흰색, 본문은 짙은 남색, 상태와 강조는 산호색·초록색을 제한적으로 사용한다.
- 카드 반경은 최대 8px, 지도 위 컨트롤은 안정된 고정 크기를 사용한다.

## 상호작용

- 길찾기 패널이 닫힌 일반 지도에서 마커 클릭은 기존 상세 패널을 연다.
- 저배율에서는 클러스터로 밀도를 낮추고, 고배율에서는 아파트 가격을 바로 읽게 한다.
- 지도 화면 주변 마커만 렌더링하고 이동 종료 때 갱신해 휴대폰 DOM 부하를 제한한다.
- 휴대폰 안전 영역과 하단 시스템 탐색 영역을 침범하지 않는다.

## 길찾기 패널

- 길찾기는 기존 상세 패널과 같은 흰색 지도 오버레이를 사용한다.
- `회사 가기`와 `퇴근하기`는 2분할 세그먼트, 출발 시각은 `datetime-local`, 위치는 장소 검색·현재 위치·지도 선택 버튼으로 입력한다.
- 추천은 도보 1개, 택시 1개, 대중교통 3개 순서로 고정하고 이동수단별 색과 아이콘을 구분한다.
- 결과 카드는 총 소요시간을 가장 크게 표시하고 대기·셔틀·도보/택시/대중교통 시간을 같은 줄의 3칸 요약으로 비교한다. 도보·택시는 시간 뒤에 실제 경로 거리를 km로 함께 표시한다. `경로 상세보기`는 별도 명령 버튼으로 제공한다.
- 후보별 도보·택시·대중교통 조회는 Worker 배치 요청 한 번으로 묶어 사용자 단위 요청 제한을 소진하지 않는다.
- 경로 상세는 출발, 첫/마지막 이동수단의 실제 안내 단계, 셔틀 대기, 셔틀 승하차, 도착 순서로 표시한다. 대중교통은 노선명·승하차 정류장·정거장 수·환승 정보를 노출한다.
- 추천 선택 시 지도에는 승차 정류장부터 하차 지점까지의 셔틀 구간만 보라색으로, 선택 위치와 정류장 사이 접근 구간은 도보 초록·택시 주황·대중교통 파랑으로 표시한다.
- 선택 위치는 다른 마커보다 큰 산호색 원형 핀과 흰 외곽선으로 표시한다. 패널은 모바일에서 하단 안전 영역 안에서 스크롤한다.
- 길찾기 패널은 검색·추천 결과·경로 상세의 세 단계만 표시하며, 현재 단계가 아닌 입력과 결과는 화면에서 숨긴다. 상단 뒤로가기는 상세→추천 결과→검색 순서로 이동하고 입력값은 유지한다.
- 길찾기 패널은 지도를 비활성화하지 않는다. 모바일 상세는 기본 `30dvh`로 지도 약 70%를 노출하고, 상세 본문을 누르면 `68dvh`로 확장하며 지도 터치·드래그·휠 줌 시작 시 다시 축소한다.
- 길찾기 패널이 열린 동안 정류장·아파트 마커는 길찾기 단계나 선택 경로를 대체하지 않는다.
- 지도 선택 중 마커를 누르면 정류장·아파트 이름을, 빈 지도를 누르면 축약 도로명주소를 위치 입력값으로 사용한다. 장소 검색 결과는 검색된 장소명을 유지한다.

## 아파트 정류장 시간

- 아파트 상세의 각 정류장은 `출근 N분 · 퇴근 N분 · 도보 N.Nkm`를 한눈에 표시한다.
- 출근은 `통상 출근`, 퇴근은 `통상 18시퇴근` 운행의 정류장-회사 소요시간을 우선한다.
- 해당 운행이 없으면 출근은 회사 도착 08:00, 퇴근은 회사 출발 18:00에 가장 가까운 운행을 쓰고 실제 운행명을 `기준` 보조문구로 표시한다.
- 각 정류장에는 도보시간을 포함한 `집 출발 → 셔틀 → 회사`와 `회사 → 정류장 → 집 도착` 시각을 함께 표시한다.
- 도보 길찾기는 1.2km 이하 후보를 먼저 비교하고, 같은 거리군 안에서 총 도착시간이 빠른 순으로 추천한다.

## 학교 정보

- 아파트 상세에는 직선거리 기준 가까운 초등학교·중학교·고등학교를 각각 3개 표시한다. 실제 배정 학교로 오해하지 않도록 제목을 `가까운 학교`로 고정한다.
- 중·고등학교의 학업성취도와 진학률은 같은 학교급·같은 공시연도 전국 분포에서 계산한 `상위 N%`를 각 지표와 분리해 표시한다.
- 공식 공시값이 없으면 순위를 추정하지 않고 `공시 자료 없음`으로 표시한다. 데이터 출처와 기준일은 상세 하단에 노출한다.
